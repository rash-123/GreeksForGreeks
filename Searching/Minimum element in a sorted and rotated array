class Solution{
public:
    int findMin(int arr[], int n){
        //complete the function here
        int minElement = arr[0];
        for(int i = 0; i < n; i++){
            if(arr[i] < minElement){
                minElement = arr[i];
            }
        }
        return minElement;
    }
};