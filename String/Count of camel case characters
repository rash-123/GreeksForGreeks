class Solution
{
  public:   
    int countCamelCase (string s)
    {
    	//code here.
    	int count = 0;
    	int n = s.size();
    	for(int i=0;i<n;i++){
    	    if(s[i] >= 'A' && s[i] <= 'Z'){
    	        count++;
    	    }
    	}
    	return count;
    }
};