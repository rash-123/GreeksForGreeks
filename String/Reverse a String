// User function Template for C++
class Solution {
  public:
    string revStr(string S) {
        // code here
        string rev;
        for(int i = S.size()-1;i>=0;i--){
            rev += S[i];
        }
        return rev;
    }
};